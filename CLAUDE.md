# 이세계 리뷰용사 (review-hero)

리뷰(악플/찬양)가 공격·경제·성장 수단인 텍스트 중심 로그라이크 덱빌더.
싱글 코어 + 비동기 온라인(다크소울 메시지 방식), 1~2인 개발, 한국어 특화.

## 문서 구조 (3층 — 역할을 섞지 말 것)

| 위치 | 역할 | 규칙 |
|---|---|---|
| `design/` | **정본** — GDD-v1.0.md(시스템), worldview-v1.0.md(세계관·용어), cards/enemies YAML(게임 데이터) | 충돌 시 GDD 우선. 용어는 worldview §6 사전 준수 (예: UI에서 "에너지" 금지, "필력") |
| `wiki/` | 탐색용 파생 뷰 (Obsidian 볼트, `[[링크]]`) | 정본을 먼저 수정하고 위키를 갱신한다. 위키에만 수정하는 것 금지 |
| `wiki/결정기록/` | ADR (Architecture Decision Record) | 설계 방향을 바꾸는 결정은 반드시 ADR 추가 — 기각한 대안과 사유 포함 |

## 커밋 규칙

**이 저장소는 게임의 기획→설계→제작→배포 전 과정의 기록이다.** 커밋 히스토리 자체가 개발 일지가 되도록, 모든 커밋 메시지를 디테일하게 작성한다.

### 형식

```
type(scope): 한 줄 요약

[무엇을] 변경 내용을 구체적으로. 수치 변경은 반드시 전→후.
[왜] 의사결정의 배경과 근거. 관련 ADR·GDD 섹션 인용 (예: ADR-006, GDD §3.4).
[과정] 시도했다가 버린 대안, 시뮬레이션·테스트 결과, 참고한 논의가 있으면 기록.
```

- type: `design`(설계 변경) | `content`(카드·적·텍스트 데이터) | `balance`(수치 조정) | `feat`(기능 구현) | `fix` | `docs`(위키·문서) | `infra`(Firebase·배포) | `chore`
- 메시지는 한국어로 작성한다.
- 한 줄짜리 본문 없는 커밋 금지. "수정", "업데이트" 같은 요약 금지.

### 타입별 추가 규칙

- `balance:` — 전→후 수치 표와 조정 이유(어떤 플레이가 문제였는지) 필수.
- `design:` — 정본(GDD/worldview) 변경 시 GDD 개정 이력에 버전 추가 + ADR 필요 여부 판단. ADR을 쓰면 같은 커밋 또는 연속 커밋으로.
- `feat:` — 구현한 규칙의 GDD 섹션 번호를 본문에 인용 (코드↔설계 추적성).
- `content:` — 카드/적 추가·변경 시 YAML이 정본, 위키 페이지 갱신 여부 명시.

### 예시

```
balance(combat): 팩트 판정 게이지 +2 → +3 상향

[무엇을] 태그 적중 시 신뢰도 게이지 획득량을 +2에서 +3으로 변경 (GDD §3.4 표 갱신).
[왜] 시뮬레이터 1,000판 결과 표준 플레이의 보스전 크리 발동률이 41%로,
설계 목표(보스전 1회 = 사실상 100%)에 크게 미달. 적중 +2는 GDD §3.4가
명시한 1순위 튜닝 레버라 이것부터 조정.
[과정] 크리 요구 게이지 10→8 하향안을 먼저 시뮬했으나 숙련 플레이에서
전투당 3회 발동해 기각. 상향 후 표준 92% / 억지 플레이 7%로 목표 부합.
```

## 개발 메모

- 빌드/테스트 명령: `npm install` → `npm test`(node:test), `npm run typecheck`(tsc --noEmit),
  시뮬레이터 `npx tsx packages/sim/src/cli.ts --enemy B01 --policy standard --runs 1000 --seed 42 [--json]`
- 패키지 구조: `packages/core`(UI 무의존 전투 상태머신, fs/network 금지) + `packages/sim`(YAML 로드·정책 AI·통계 CLI)
- 전투/런 로직은 UI 무의존 TS 상태머신 패키지로 분리 (GDD §1.1) — 서버 리플레이 검증에 재사용되므로 `Date.now()`·`Math.random()` 직접 호출 금지, 시드 주입.
- 서버: Firebase (Auth 구글 로그인 + Firestore + Cloud Functions/Scheduler). 유의점은 ADR-008.
