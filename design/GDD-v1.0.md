# 이세계 리뷰용사 — GDD v1.0 (시스템 설계 정본)

- 작성: 리드 디자이너 (설계 회의 최종 종합)
- 지위: **이 문서가 모든 회의 산출물(01~06, 10)과 v0.1 문서를 대체하는 단일 정본이다.** 충돌 시 본 문서 우선.
- 반영 입력: meeting/01~06, 10_interactions(해소안 R01~R23), 적대적 검증 3종(악용/온보딩/실현성).
- 판정 근거는 부록 A(지적사항 판정표), 부록 B(R 승인 현황) 참조.
- 개정 이력: v1.0.1 (2026-07-30) — 서버 스택 Supabase → **Firebase** (사용자 결정, wiki/결정기록/ADR-008 참조).
- 개정 이력: **v1.1 (2026-07-31) — 밸런스 라운드 1**: balance-report-v1 제안 7건 전건 채택 + GDD 침묵 규칙 6건 명문화 (사용자 결정, ADR-009). 변경: 팩트 게이지 +3(§3.4), 정예 의지 상향(YAML), 페이즈2 비례 트리거(§3.8), 보스 길이 기준선 6~7턴(§3.1), 바이럴 크리 바닥 보장·불편러 크리 재설계(§3.5), 퇴고 액션(§3.2), 보스 표준 검증 덱(§3.6).

---

## 0. 확정 사항 (불변)

- 장르: 리뷰가 공격·경제·성장 수단인 텍스트 중심 로그라이크 덱빌더. 싱글 코어 + 비동기 온라인. 1~2인 개발. 한국어 특화.
- 전투: 접두(대상 측면+태그) + 접미(판정+효과) 조합. 팩트 ×1.5/게이지+2, 일반 ×1.0, 헛소리 ×0.5/게이지−2. 게이지 10 → 성향별 크리티컬 리뷰.
- 4계열: 품질/마감(물리), 성능/최적화(마법), 배송/CS(시공간·CC), 감성/디자인(정신). 원소 상성 없음(태그 적중/무효로만 표현).
- 경제: 리뷰 네고(악평 할인 + 런 지속 페널티, 런 종료 시 소멸).
- 비동기: 보스 디버프 리뷰 공유(슬롯 5, 구매 인증, 배당, 사장님 답글) + 유언·전단지.
- UGC: 등록 시점 LLM 1회 판정. 전투 중 실시간 LLM 금지.
- 개발 순서: Layer1 전투 → Layer2 네고 → Layer3 비동기 → Layer4 UGC. 서버 = Supabase 수준 CRUD.

---

## 1. 제품 정의·개발 로드맵·MVP 컷라인

### 1.1 기술 스택 (확정)

- 클라이언트: **TypeScript + React(또는 Svelte)**. 전투/런 로직은 **UI 무의존 순수 상태머신 패키지**로 분리(렌더러 교체 가능). 텍스트 중심이므로 게임 엔진·Canvas 불사용.
- 이식 경로: 웹 프로토타입 → 스팀 출시 시 Tauri(1순위) 또는 Godot 4. Unity 배제.
- 서버: **Firebase** — Auth(구글 로그인), Firestore(데이터), Cloud Functions(검증·LLM 프록시·정산), Cloud Scheduler(배치). Realtime DB·Storage 미사용. 보안 규칙: 본인 문서만 쓰기, 읽기는 공개 컬렉션. (v1.0.1: 회의안 Supabase에서 사용자 결정으로 변경 — ADR-008)
- 운영비 상한: 서버 ~$25/월 + LLM $50/월 = 월 약 10만원.

### 1.2 MVP (Layer 1) 컷라인 — 목표 3~4개월

**MVP = "1막 완결 싱글 런". 이 범위 밖은 전부 이후 마일스톤.**

| 포함 | 제외 (도입 시점) |
|---|---|
| 1막 6층(일반층 5 + 보스층 1), 층당 분기 노드 2~3개 | 2·3막, 보스 2종, 일반 적 추가 4~6종 (Layer 1.5) |
| 노드: 전투 / 이벤트 / 상점(카드·제거·포션만) / 휴식 | 게시판 노드 (Layer 3) |
| 적 E01~E05 + B01(의지 60) | 업보 노드·유언 봉인·공양 (출시 후) |
| 카드 40장(X09 제외) + 시작 덱 12장 + 시작 장비 3종 | 재화 RP/P, 티어, 해금 트리 (Layer 1.5에서 P만) |
| 태그 판정·게이지·크리 4종·성향·항복 | 네고·판매·진상도·X09 (Layer 2) |
| 온보딩 1판 보정(§4.4) | 주간 시드·리더보드 (Layer 3 v1.1) |

싱글 코어(Layer 1~2)는 네트워크 없이 100% 동작해야 한다(검수 기준).

### 1.3 로드맵

| 마일스톤 | 내용 | 기간 목표 |
|---|---|---|
| **Layer 1 (MVP)** | §1.2. 전투 엔진 6주 + 콘텐츠·튜닝 6주 | 3~4개월 |
| **Layer 1.5 (풀 런)** | 2·3막, 보스 2종(2막 의지 ~60 / 3막 ~80+페이즈2), 일반 적 4~6종, B01 의지 48 하향(재시뮬 후), 게시판 노드(프리셋 구동), P 해금 트리(375P), 명성(RP) 카운터 | +2개월 |
| **Layer 2 (경제)** | 리뷰 네고, 5성 판매, 진상도, 악평 토큰, X09 추가(41장), 협찬 수락 | +1.5개월 |
| **Layer 3 v1 (비동기)** | 계정, 보스 리뷰 슬롯 5 경쟁, 좋아요, 사장님 답글 연동, 유언(템플릿), 승리 작성권, 서버 신뢰 모델(§8) | +2개월 |
| **Layer 3 v1.1** | 처치 배당, 전단지, 주간 시드+리플레이 검증, 리뷰어 랭크(티어), 시즌(12주) | 출시 후 |
| **Layer 4 v1 (UGC)** | 금지어 팩폭(자유문 1구간) + LLM 파이프라인 | +1.5개월 |
| **Layer 4 v1.1** | 유언 자유문, 전단지 자유문, 박제 파이프라인 | 출시 후 |
| **컷(무기한 보류)** | 사기극(허위 바이럴 의뢰) 전 구간, 계정 골드, 게시판 조롱 버튼, 고집쟁이 장인 NPC, 엔딩 B·C 구현 | 지표 확인 후 재검토 |

전체 출시 목표: 12~15개월.

---

## 2. 공통 계산 규칙

1. 모든 배율은 **내림, 최소 1**. (3딜×1.5=4, 5딜×0.5=2)
2. 신뢰도 게이지 **0~10, 초과 소실, 하한 0**. 전투 시작 0(외부 보정 제외), 전투 간 미유지.
3. 에너지(UI 표기 **"필력"**, 이하 규칙 서술은 편의상 에너지): 턴당 3, 이월 없음. S15는 다음 턴 가산.
4. **런 시작 외부 보정 총합 캡(R20)**: 비동기·메타 유래 보정은 시작 게이지 +3, 시작 골드 +30, 보스 디버프 위력 합산 +50%를 초과 불가.
5. **계열 콤보는 폐지**(v1.0). 게이지 소스는 §3.4의 표가 전부다.
6. 용어는 worldview-v1.0.md §6 용어 사전이 정본. 코드·문서는 사전 키를 참조한다(문자열 하드코딩 금지 — "필력" 채택 조건).
7. **피해의 인게임 명칭은 「좋아요」다**(worldview §1.1 호응 원칙 · §6 · ADR-015). 규칙 서술은 편의상 '피해'로 적되, **UI·플레이버·연출 텍스트는 예외 없이 「좋아요」를 쓴다.**

**좋아요 환산식 (모든 피해의 단일 경로):**

```
최종 좋아요 = ⌊ 기본 좋아요 × 판정 배율 × 기타 배율(버프·상성·기믹) ⌋ + 고정 가산
             (규칙 1 적용 — 내림, 최소 1)
```

- **기본 좋아요** = 카드에 인쇄된 수. 아직 아무도 누르지 않은 잠재치이며 UI에서 **빈 👍**로 표시한다.
- **최종 좋아요** = 환산 결과. 실제로 눌린 수이며 **채워진 👍**로 표시하고, 이 값이 의지·내구도를 깎는다.
- 배율은 전부 곱한 뒤 **한 번만** 내린다(중간 내림 금지 — 원산지 ×1.5와 버프 ×1.5가 겹칠 때 오차 방지).
- **고정 가산은 내림 뒤에 더한다**(원산지 +1, X05 가산 등). 배율의 영향을 받지 않는다.
- 적의 공격도 같은 단위다. 적의 리뷰가 받은 좋아요가 플레이어의 의지를 깎는다.

---

## 3. 전투 시스템 (Layer 1)

### 3.1 기본 수치

- 플레이어: 의지 30, 에너지 3/턴, 손패 5장 유지(상한 8장, 초과분 드로우 중단·소멸 없음), 손패 유지형.
- 적: 의지 + 장비 1~3개(내구도·태그) + 인텐트 공개 패턴.
- 전투 길이 기준선: 일반 2~4턴 / 정예 5~6턴 / 보스 **6~7턴**(v1.1 — 플레이어 의지 30·B01 화력 현행에서 8~10턴은 7턴 사망 벽으로 구조적 불가, 기준선 현실화. 제안 4a). 플레이어 화력 **턴당 좋아요 8~12**. 적 화력 일반 4~6.

### 3.2 턴 시퀀스 (01 §1 승계 + 수정)

```
[전투 시작] 셔플 → 5장 드로우 → 게이지 = 0 + 외부 보정(캡 +3) − 감성 악평 페널티 p → 인텐트 공개
[플레이어 턴]
 1. 턴 시작: 내 도트 발동·만료, 예약 효과(X05)
 2. 드로우: 5장까지
 3. 에너지: 3 + 보정
 4. 플레이: 리뷰 제출(접두+접미) / 특수 / X06 설치 / **퇴고(v1.1: 필력 1 — 손패 1장을 버리고 1장 드로우. 턴 제한 없음, 뽑을 카드가 없으면 불가)**. 크리티컬은 별도 버튼(에너지 0, 게이지 전량, 턴당 1회)
 5. 턴 종료 선언
[적 턴]
 6. 행동: 인텐트 순서 실행. X01 지연 시 스킵. 공격 선언 시 X06 대기 중이면 자동 발동
 7. 정리: 지속효과 tick, 기믹 카운터, 다음 인텐트 공개
```

- **기절 규칙(신규)**: 기절한 적은 그 턴 type: attack/buff/steal/stealth 행동 불가. **type: gimmick(사장님 답글 등)은 기절 무시하고 발동**. 기절에서 깨어난 적은 **1턴간 경직 내성(기절·지연·장비 비활성화 면역)**. → S09 무한 락 봉쇄.
- **장비 비활성 봉인(v1.1 명문화)**: 활성 장비가 하나도 없는 적은 그 턴 비(非)기믹 행동이 봉인되며, 봉인 불발된 적은 재활성 후 1턴간 경직 내성을 얻는다. → S07 매턴 재시전 무한 락 봉쇄.
- **퇴고(v1.1)**: 손패 교착(접두 없는 손패 고착 — 구판 강제 패배 ~2%)의 안전장치. 재화·판정과 무관한 범용 행동.
- **은신 게이트와 카드 종별(v1.1 명문화)**: 크리티컬 리뷰도 "리뷰"이므로 E04 은신 게이트를 적용받는다(적을 향하지 않는 바이럴 크리 제외. 배송 크리는 명중+은신 해제). 특수 카드는 리뷰가 아니므로(무판정) 게이트 비대상.
- X06 리액션: 01 §1.1 그대로 (설치형, 대기 슬롯 1, attack에만 발동, −50% 후 피해의 50% 반사, 전 히트 적용·1회 소진).

### 3.3 태그 판정

| 판정 | 조건 | 좋아요 배율 | 게이지 |
|---|---|---|---|
| 팩트 | 접두 태그 ∈ 대상 태그 | ×1.5 | **+3** (v1.1) |
| 일반 | 무관 | ×1.0 | 0 |
| 헛소리 | 접두 태그 ∈ 대상 무효 태그 | ×0.5 | −2 |

- **무효 태그는 적 검사 UI에 "평가 불가 항목" 라벨로 전 구간 상시 노출한다**(숨김 정보 아님 — 읽고 조합하는 게임). 헛소리 발생 시 피드백 1줄: "이 대상엔 [연비] 항목이 없습니다."
- **버프 접미(S13~S16)는 반드시 내 장비 1개를 대상으로 지정**(자기 자신 대상 폐지 — 01 제안⑥). 접두 태그 vs 내 장비 태그로 동일 판정. 적중 시 수치 ×1.5(내림)+게이지 +2. **온보딩 1판 한정 버프 접미는 무판정(항상 일반)**, 2판부터 판정 개방.
- 다중 히트(S03)의 판정·게이지는 히트당 적용하되, **버프 가산(S13 등)은 "제출당 1회"만 적용**(히트당 아님).

### 3.4 신뢰도 게이지 — 소스/싱크 전량표

| 소스 | 값 | 싱크 | 값 |
|---|---|---|---|
| 팩트 판정 | **+3** (v1.1) | 헛소리 판정 | −2 |
| X08 별점 구걸 | +2 | S04 / S11 / X01 | −1 |
| 재반박 성공(L3) | +1 | X02 | −2 |
| 감성 디버프 리뷰(L3, 캡 +3) | +1~3 | E05 찬양 강요 | −1 |
| X09 결함의 무기화(L2) | +1 | 크리티컬 사용 | 전량(−10) |
| 티어 혜택(v1.1, 캡 내) | +1 | 감성 악평 토큰(L2) | 시작 −p |

계열 콤보 폐지 후 기대 수급(판정 1.8회/턴): 표준(적중50%/헛소리10%) +1.44/턴 → 보스전 7턴째 크리 1회. 숙련(75%/5%, 판정 2.0) +2.8/턴 → 4턴·8턴 크리 2회. 억지(30%/25%) +0.2/턴 → 0회. **v0.1 목표(크리 = 정예·보스전 이벤트) 유지 확인. 적중 게이지가 1순위 튜닝 레버.**

v1.1 실측(밸런스 라운드 1 적용 후, balance-report-v2): 이론 수급의 병목이 계수가 아닌 손패 가용성(폴백 42~47%)임이 확인되어 팩트 +3으로 상향. 재실측 — B01전 크리 발동 표준 0.80회(목표 1 근접) / 숙련 0.93회 / 억지 0.67회. 숙련 2회·억지 0회 목표는 손패·덱 구성 레버(라운드 2 과제) 없이는 미달.

### 3.5 크리티컬 리뷰 (게이지 10, 에너지 0, 턴당 1회)

> **UI 표기는 「이 라운드 베스트 리뷰」다** — 그 라운드에서 좋아요를 가장 많이 받은 리뷰라는 뜻이다.
> 리뷰 1건에 붙는 한시적 표식이며,
> 최종 보스의 직함 「베스트 리뷰어」(사람)와 층위가 다르다(worldview §1.2·§6).
> 플레이어가 매 전투 표식을 모으는 행위가 엔딩 B로 가는 예행연습이라는 회수가 여기서 시작된다.

| 계열 | 성향 | 효과 | 확정 수치 |
|---|---|---|---|
| 품질/마감 | 팩트 폭격기 | 방어·저항 무시 **고정 좋아요** | 20 |
| 성능/최적화 | 힙스터 평론가 | 적 공격력 −50% **디버프 부착**(사장님 답글의 반박 대상 — 카운터플레이 존재) | 전투 내 지속 |
| 배송/CS | 프로 불편러 | 전체 기절 1턴 + **다음 행동 위력 −50%**(v1.1 — 기절 면역·기믹 대상에도 가치 보장) + 골드 갈취 | 갈취 일반 8G / 정예 15G / 보스 25G — **갈취만 전투당 1회**, 기절·위력 감소는 크리마다 |
| 감성/디자인 | 바이럴 앞잡이 | 현재 내 버프 효과 2배. **버프 0개면 대신 S13 상당(+3) 가산 버프 1개 즉시 부착**(v1.1 바닥 보장, 부착 슬롯 미점유) | **가산 버프 합산 상한 +12.** 크리 2회 중첩·바닥 버프 포함 상한 공유(승산 폭주 봉쇄) |

성향 결정: 접두 계열 누적 카운터 argmax. 동률 = 최근 제출 계열(v1.1 명문화: 동률군에 최근 제출 계열이 없거나 null이면 품질→성능→배송→감성 선언 순), 초기값 = 팩트 폭격기. **전투 시작 시 스냅샷 고정.** 성향 UI(아이콘·카운터·맵 배지)는 **2판부터 해금** — 1판은 "필살 리뷰 준비 완료" 버튼 하나로 축약.

### 3.6 덱 규칙

- 시작 덱 12장(01안 정본, R06): P01/P05/P09/P13/P02 + S01×2/S03/S05/S13/S14 + X08. 이 중 **접두 4장(P01/P05/P09/P13)+접미 4장(S01×2/S03/S05)은 "생계형 리뷰" 표식 — 제거·판매 불가** → 덱 하한(총 10, 접두≥4, 접미≥4) 자동 보장.
- 획득: 일반 3택1 / 정예 4택1(special 가중 2배) / 보스 4택1+특수 확정 1. **시작 덱·1막 보상 풀에서 S04/S11/X01/X02(게이지 감소 카드) 제외.**
- 제거: 상점 25G, 회당 +10G, 상한 45G, 상점당 1회(R07).
- 순환: 제출 카드는 묘지 → 덱 소진 시 묘지 셔플. **X04로 증정한 카드는 "런 동안" 제외**(전투 후 복귀 폐지).
- **유니크(덱 1장 제한) 카드: X08, X04, S09, X09.**
- **시뮬 검증 표준 덱(v1.1, 보스)**: 시작 덱 12장 + 보스 약점 계열 접두 1장 + 대응 카드 2장 (B01 = P11/P15/S02). 밸런스 검증·R11류 재시뮬·리플레이 검증의 기준 덱. "보스 도달 기대 덱"의 정본 정의.

### 3.7 카드 변경표 (cards-v0.1 → v1.0)

| 카드 | 변경 | 사유 |
|---|---|---|
| X08 | 개명 「별점 구걸」(플레이버 유지), 유니크 | 비동기 "좋아요"과 용어 충돌 해소, 억지 플레이 게이지 우회 차단 |
| X04 무료 나눔 | 유니크 + 증정 카드 런 제외(실비용 부여). 배율 ×4 유지 | 1에너지 12딜 무한 반복 차단(악용 #4). 1회성 폭발로 재미 보존 |
| S09 고소합니다 | 유니크. 기절 룰(§3.2)로 연쇄 락 불가 | 영구 기절 락 차단(악용 #3) |
| S03 | 효과 텍스트를 "의지 3, 2회 판정"으로 재서술 | 개발자 주석 톤 제거(UX #6) |
| 특수 8종 | 카드에 "무판정" 뱃지 표기 | yaml 주석에만 있던 규칙의 카드 자립화 |
| 접두 전체 | 하단 고정 서식 "태그 [마감] — 대상의 리뷰 항목과 대조" | 태그=장식 오독 방지(UX #6) |
| 버프 접미 | "내 장비에게" 대상 아이콘 필수 | 대상 반전 혼란 방지(UX #1) |
| X09 결함의 무기화 (Layer 2 신설, 41장) | 비용 1, Σp(상한 8)×3 의지 데미지, 게이지 +1, **전투당 1회**, 유니크 | 네고 페널티→화력 반전 연결 카드(02 C1 + R02) |

### 3.8 적 데이터 변경표 (enemies-v0.1 → v1.0)

| 대상 | 변경 |
|---|---|
| E01 바가지 씌우기 | "골드 하한 0" 명기(02 C2) |
| B01 사장님 답글 | "반박·제거" → **"이번 전투 한정 정지"**(03 제안1). 대상 선정: **출처 무관, 활성 디버프 전체 중 Tier/위력 최상위**(동률 시 좋아요↑→최근) — 힙스터 크리는 Tier3 상당으로 최우선 반박 대상(R22). 디버프당 반박 1회, 재반박(같은 계열 팩트) 1회로 부활+게이지+1 — **재반박 조건은 "같은 계열 팩트 판정 리뷰 제출"이며 리뷰의 대상은 불문**(v1.1 명문화). 반박 대상 없으면 의지 +5만. **기절 중에도 발동** |
| B01 페이즈2 트리거 | **"의지 30 이하" → "의지 50% 이하" 비례식**(v1.1, 제안 3). 현행 의지 60에선 30=50%라 무변화. 의지 하향 배치(R11) 시 완화가 실제로 작동하는 필요조건 |
| B01 on_defeat | 3등급 인증(§6.1)으로 확장 + **승리 시에도 작성권 발생**(§6.1) |
| B01 의지 | MVP(1막 완결 런의 최종 보스) = **60 유지**. **R11 해소(v1.1)**: 페이즈2 비례 트리거와 함께면 48 하향이 실제 완화로 작동(재시뮬 승률 86.0% > 60 기준 83.2%) — Layer 1.5 배치 시 적용 가능 |
| B01 스키마 | `banned_words: [환불, 응대, 답변, 고객센터, 사장]` 추가(Layer 4용, 04 제안1) |
| 온보딩 1판 첫 전투 | E01 변형판: **무효 태그 없음**(팩트/일반 2종만으로 판정 학습) |

### 3.9 플레이어 장비

슬롯 3(무기/방어구/장신구), 내구도 없음. 공용 장비 스키마(R23): **태그 1~2, 무효 태그 0~2, 패시브 0~1** + 고정 리뷰 1줄 + 부착 슬롯 2칸. 상점 판매품 권장 기본값은 태그 2·패시브 1, 시작 장비 3종(01 §3 표 그대로)은 패시브 없음 하한 예외. 지속 버프(S13)와 악평 토큰(L2)은 **부착 슬롯 2칸을 공유**(R15) — 꽉 차면 추가 부착 불가.

---

## 4. 런 구조·메타 (Layer 1~1.5)

### 4.1 런 골격

3막 × 6층(일반 5+보스 1) = 18노드. 층당 노드 2~3 분기, 층당 1개 방문. 보스 직전 층은 휴식(1.5 이후: 또는 게시판) 보장. **MVP는 1막만.**

노드 가중치(%): 05 §1.2 표 승계. 단 MVP는 게시판 슬롯을 이벤트로 대체. 동일 유형 3연속 리롤. 목표 플레이 타임: 풀 런 20~30분, MVP 1막 런 8~12분.

### 4.2 재화 — 2계통 확정 (R03)

> **메타 진행의 서사 근거(worldview §1.7).** 해금으로 늘어나는 것은 수치가 아니라
> **쓸 수 있는 말의 수**다. 위력은 좋아요가 정하고 좋아요는 대상에 정확히 들어맞는 말에 눌리므로,
> 표현이 모자라면 어떤 대상 앞에서도 두루뭉술해지고 두루뭉술한 글은 눌리지 않는다.
> **앞선 원정대원들이 실패한 이유가 이것이며, 플레이어가 카드를 늘리는 행위가
> 곧 주인공이 그자에게 닿는 유일한 길이다.**

| 재화 | 용도 | 소스 | 싱크 |
|---|---|---|---|
| **골드** | 런 화폐 (런 종료 시 소멸) | 전투 15G(일반)/24G(정예)/보스 50G(1막), 항복 +6G, 갈취, 판매, 이벤트 | 카드 25/45G, 제거, 포션 12G, 회복 25G, 장비 60/90G(L2), 진열대 25~45G(L3) |
| **명성(RP)** | 명예·노출·랭크 (시즌 12주, 30% 이월) | 보스 +10/막, 클리어 +30, 사망 시 **신규 최고 도달 막 갱신 시에만** 막×5, 슬롯 유지 +2/일, 배당 +1/처치, 유언 좋아요 +2(일 상한 20), UGC 채택 +3(일 상한 30) | 랭크 문턱, 시즌 감가 |
| **적립금(P)** | 카드 해금 (Layer 1.5). 인게임 표기 **「협찬 수령」** | 막 +8, 클리어 +15, 정예 +2, 온보딩 +5 | 해금 트리 375P + X09 40P. 완주 후 장기 싱크는 코스메틱 트리(v1.1) |

> **협찬 수령 규칙 (서사 정합).** P로 해금한 카드에는 **원산지 보너스가 영구히 붙지 않는다**
> — 전생 카드(Z01~Z12)와 같은 취급이다. 겪지 않고 돈으로 얻은 리뷰이기 때문이다.
> 이것은 주인공을 죽인 바로 그 짓(협찬 = 안 겪고 쓴 리뷰, worldview §2.1)이며,
> **"돈으로 산 리뷰는 끝내 팩트가 되지 못한다"** 를 규칙 한 줄로 성립시킨다.
> 원산지 보너스는 오직 직접 이긴 대상에게서만 얻는다(card-system-v2 §2).

**"계정 골드" 폐기.** 배당은 P로 지급(10G→1P 환산, 일 상한 30P). RP 지급 단일표는 본 표가 소유 — 타 문서의 +5/+500 등 수치는 전부 폐기.

- 1막 수지(R01 재산정): 수입 ≈ 일반 3×15 + 보스 50 + 이벤트 10 + 갈취 8 ≈ **110G**, 상점 1회 지출 기대 60~90G. 풀 런 수입 ≈ 340~360G / 싹 250~400G.
- 휴식 노드: 의지 **30%** 회복 or 카드 1장 무료 제거, 택1(R09).
- 리뷰어 랭크(구 "신뢰도 랭크") 4티어·주간 시드·시즌: **v1.1로 연기.** UGC/좋아요 자격 게이트는 티어 대신 **"첫 보스 처치 계정"** 단일 조건(04 게이트 승격).

### 4.3 유언·업보 (조정)

- 유언 작성: 모든 사망 시 가능(Layer 3, 템플릿만). **좋아요 보상 집계는 "그 런에서 전투 승리 1회 이상"인 런의 유언만**(자살 파밍 차단, 악용 #8 완화 수정).
- 업보 노드·카드 봉인·공양: **출시 후로 연기.** 도입 시 업보 승리 골드는 사망 막 비례(1막 +12G / 2막 +21G / 3막 +30G), 공양 반환 카드는 해당 런 사용 불가.

### 4.4 온보딩 — 1판 개념 11개로 다이어트 (UX 검증 채택)

> **선행 단계 — 만물대장 리뷰어 등록(프롤로그 마지막 슬라이드 P15).** 개념 카운트에 넣지 않는다.
> **이름과 서명을 한 장에서 함께 받는다**(ADR-020) — 화면을 나누지 않는 이유는 그것이
> 한 번의 행위이기 때문이다. 플레이어가 적어 넣는 필명이 만물대장에 올라가는 작성자명이며,
> **이 세계의 이름을 쓰면 되받아 맞는다**는 것이 주인공이 살아남는 유일한 근거다
> (worldview §2.1·§2.2, ADR-016·019).
> **둘의 역할이 다르다** — **이름은 사람들이 보고**(리뷰어를 구분하는 표지),
> **서명은 대장이 대조한다.** 서명은 **올리는 행위 그 자체**이며 독자는 보지 않는다.
> 그래서 전투에서 카드에 서명하는 모션이 곧 「게시」다 — 서명이 그어지는 순간 그 리뷰가 올라간다.
> 저장 위치 `localStorage['reviewhero.penname']` · `['reviewhero.sig']`.
> 이후 모든 화면이 이 이름을 부르고 모든 카드 하단에 이 서명이 그어진다.

| 판 | 도입 개념 | 보정 |
|---|---|---|
| **1판** | 조합 → 팩트/일반(첫 전투 무효 태그 없는 E01 변형) → 헛소리(원인 1줄 표시) → 게이지 → 크리(성향 고정, "필살 리뷰" 버튼) | 적 공격 ×0.75, 헛소리 −2→−1, 보스 약화판(의지 45, 야근 강요 미사용), 버프 접미 무판정, 성향 UI 미노출, 항복 미도입 |
| **2판** | 항복(E02에서 자연 발견) → 버프 태그 판정 → 정예 → 성향 UI | 적 공격 ×0.9 |
| **3판~** | 정상 난이도. 이후 레이어 도입 순서: 네고(L2) → 게시판·보스 리뷰·유언(L3) → UGC(L4) | 없음 |

---

## 5. 경제/네고 (Layer 2)

02 문서를 다음 수정과 함께 승계한다.

1. **런 구조는 §4.1이 정본**(R01). 02의 수지표는 폐기, §4.2 수치로 대체. 1막 상점 진열 = 장비 1 + 카드 3.
2. 할인율 = 접미 기본률(0코 8% / 1코 12% / 2코 18% / 3코 25%) × 판정 배율(팩트 1.5 / 일반 1.0·진상도+1 / 헛소리 0·진상도+2) × NPC 배율. 아이템당 악평 2건, 합산 50% 상한. 구매 포기해도 진상도 유지.
3. **감성 계열 악평 페널티 = "전투 시작 게이지 −p"**(R02 — "요구치 10+p"는 크리 영구 봉인 버그로 폐기). 나머지 계열 페널티는 02 §3 표 유지.
4. 악평 토큰: 장비당 2개·전체 4개 상한(A6), 부착 슬롯 공유(R15). X09 연동은 §3.7.
5. 판매·5성 사기·NPC 3종·진상도(보스 −3, 사과문은 **이벤트 한정** — 상시 세탁 금지): 02 승계. 고집쟁이 장인은 컷(보류).
6. 데스 스파이럴 안전장치 A1~A7 승계(A1 수치는 §3.6 정본으로 정정, A4는 30%).
7. 테스트 게이트: 02 §7 + "§4.1 구조 기준 재검증" + "네고 0회 런 클리어율 차이 15%p 이내".

---

## 6. 비동기 (Layer 3)

03 문서를 다음 수정과 함께 승계한다. v1 범위: 슬롯 경쟁·좋아요·사장님 답글·유언. **배당·전단지·주간 시드는 v1.1.**

### 6.1 작성권 — 승리 시에도 발생 (악용 #2 수리)

| 경로 | 조건 | 가중치 W | 명칭 |
|---|---|---|---|
| 패배 | 깎은 의지 30~59 / 60~89 / ≥90% | 1.0 / 1.5 / 2.0 (Tier 1/2/3) | 구매 인증 / 헤비 유저 / 최후의 증인 |
| **승리(신규)** | 처치 | **0.8** (Tier 1 상당 디버프) | **정복 후기(구매 확정)** |

"이길수록 손해" 역인센티브 제거. 깎은 % 는 누적 가한 피해 기준. 유저당 보스당 주간 1회, 재작성 시 좋아요 0 리셋 + **신선도 F=0**(최초 작성 1회만, E08).

### 6.2 슬롯·점수·디버프

- 슬롯 5개 활성. `score = V×W + F`, F = max(0, 48−경과h)×0.5. 매시 배치 재계산. 창의성 점수는 순위에 불개입(R05).
- 디버프 최종 위력 = `floor(풀 기본치 × Tier계수(0.8/1.0/1.25/1.5) × m(0.85~1.35))`, **절대 상한 = 기본치 ×2.0**. 게시판 좋아요 +10%는 최종값에 1회 곱(R05).
- **동일 계열 디버프는 슬롯 내 1개만 효과 적용**(악용 #6 — 03의 "2개까지"에서 강화. 3번째부터는 표기만). 감성(시작 게이지) 총합 캡 +3(R20).
- 좋아요: votes 테이블 PK = (review_id, voter_id) **채널 무관 단일**(E07). **좋아요 집계 자격 = 첫 보스 처치 계정**(악용 #7). 동일 IP/디바이스 클러스터 좋아요 가중 감쇠 + 상호 좋아요 그래프 주간 배치 탐지는 v1.1.

### 6.3 배당 (v1.1)

- 처치당 10G 상당 → **P 환산 지급**(R03). 분배 C_i = W_i×(1+V_i/10).
- **지분 0 처리: killer 본인 + killer와 동일 IP/디바이스 클러스터 계정**(E03 확장, 악용 #2 링 차단). **작성자별 주간 배당 상한** 신설(P 60/주).
- 복수 보너스 ×1.1 유지. 일일 배치 정산.

### 6.4 유언·전단지·게시판·원정대 명단

> **서사 근거(worldview §1.7).** 비동기로 만나는 타 플레이어는 **「환불원정대」의 다른 대원**이다.
> 전원 다른 세계에서 불려 왔고 전원 익명이며, 살아 있는 것은 지금 플레이 중인 자신뿐이다.
> 죽은 대원이 남긴 글을 읽으며 나아가는 구조가 다크소울식 메시지와 그대로 맞물린다.

**원정대 명단 (신규 — Layer 3 v1)**

| 항목 | 표시 | 근거 |
|---|---|---|
| 대원명 | 필명 | 대장에 올라간 작성자명 그대로 |
| 순위 | **명성(RP)** | 대원 본인에게는 별점이 없다 — 대장에 그를 **대상**으로 한 카드가 없기 때문(§2.2). 명성이 유일한 지표다 |
| 대표 리뷰 | 그 대원이 남긴 최고 좋아요 리뷰 1건 + **그가 매긴 ★** | 별점은 대원이 **받은** 것이 아니라 **매긴** 것이다. 이 구분을 UI 문구로 명시할 것 |
| 상태 | 계류 / 게시 / 유실 | 마지막 글이 올라갔는지 여부. 「유실」은 그자에게 묻힌 경우 |

- 명단 정렬은 시즌 RP 기준이며 §6.2의 좋아요 집계를 그대로 쓴다(별도 채널 금지).
- 주간 라운드 상위 100 = "이주의 체험단", 시즌 RP 상위 100 = "차기 후보석" — 명칭 변경 없음(R12).
- **표기 주의**: 명단·게시판 어디에서도 대원에게 ★를 매긴 것처럼 보이면 안 된다.
  ★ 옆에는 항상 대상(적·구성품)이 붙는다.


- 유언: 템플릿 조합만(v1). 노출 = 03 §2.2(방당 3, 층당 5), 자유문·w 가중은 Layer 4 v1.1(R10). 좋아요 보상 = RP +2(§4.2 단일표).
- 전단지 = "네고 30%+ 성사 시 상점 벽보"(03 정의 정본, R04). 게시판 ③은 **"진열대"** 로 개명, UGC 카드 가격 25~45G(R16). 04의 자기 홍보문은 전단지의 Layer 4 자유문 확장으로 통합.
- 게시판 노드 4섹션 승계, 단 **② 조롱 버튼 컷**(E09 — 페널티 증발 구조, 수리안 기각·기능 제거). ①의 좋아요 위력 +10%는 §6.2 좋아요과 동일 votes 행으로 집계(이중 채널 금지).
- 주간 라운드(구 "시즌=주간") 상위 100 = "이주의 체험단"(코스메틱만). 시즌(12주) RP 상위 100 = "차기 후보석"(R12).

---

## 7. UGC (Layer 4)

04 문서를 다음 수정과 함께 승계한다. **v1 = ③ 금지어 팩폭 1구간만.** ② 유언 자유문·④ 전단지 자유문은 v1.1, **① 사기극은 컷(보류)**.

- LLM: claude-haiku-4-5, structured outputs, system ~4,500tok 캐시, 효과는 **고정 풀 select-only + 배율 m**(창작 금지). 태그는 12종 사전 confidence 규칙. 비용 모델·스팸 방어 6종(서명 토큰·쿼터 5건/일·레이트리밋·해시 캐시·첫 보스 게이트·밴 에스컬레이션) 승계.
- **E10 표절 수리**: 해시 캐시 히트 시 최초 등록자만 원점수, 타 계정은 originality 0 + specificity 절반("표절 리뷰" 연출). LLM 호출 0회 유지.
- 모더레이션: 04 §4 승계, 단 **브랜드 패러디는 06 기준으로 강화**(R17) — "믹도랄드"류 1글자 변형 금지, 식별 불가능한 일반화 치환만 제시.
- 박제: 3중 게이트(90점 + 30일 좋아요률 상위 5% + 주간 수동 큐레이션 3장) 승계, v1.1. 골드 보상 없음 원칙 유지. 큐레이션에 원작자 확인 항목 추가(E10).
- 쿼터 = min(랭크 쿼터, 시스템 상한 5)(R14). 랭크 도입 전엔 상한 5만.

---

## 8. 서버 신뢰 모델 (악용 #1 — Layer 3 착수 게이트)

클라 보고를 신뢰하지 않는다. Layer 3 v1 착수 전 필수 구현:

1. **서버측 클램프·재계산**: dmg_pct는 0~100 클램프 + cert_tier는 서버가 재계산. best_score·팩트 수는 이론 최대치(런 구조로부터 계산) 초과 시 거부.
2. **작성권 서명 토큰**: 런 결과 요약(시드, 노드 경로, 전투 로그 해시) 제출 → 휴리스틱 정합 검사(소요 시간, 턴 수 하한) 후 발급. TTL 10분.
3. **주간 시드 리플레이 검증(v1.1)**: 시드가 결정적이므로 `시드+입력 로그` 업로드 → Cloud Function이 코어 상태머신(UI 무의존 패키지 재사용)으로 결정적 리플레이. **상위 100 + 무작위 표본만 검증** — 전수 검증·ML 이상 탐지는 하지 않는다(1인 규모).
4. 스키마: 03 §5 승계 + boss_reviews에 `crit_used bool`(R18), 승리 작성권용 `origin('defeat'|'victory')` 컬럼 추가.

---

## 9. 부록 A — 적대적 검증 판정표

### A-1. 악용 유저 관점

| # | 지적 | 판정 | 반영/근거 |
|---|---|---|---|
| 1 | 클라 전면 신뢰·리더보드 위조 | **채택** | §8. 클램프+토큰 즉시, 리플레이 검증은 주간 시드와 함께 v1.1. 단 "일반 런 통계 이상 탐지" 고도화는 기각(1인 규모 초과 — 상한 클램프로 갈음) |
| 2 | 고의 패배 지배 전략 + 배당 링 | **채택** | 승리 작성권 W 0.8(§6.1), IP/디바이스 클러스터 지분 0 + 주간 배당 상한(§6.3, 배당 도입 시점 v1.1에 적용) |
| 3 | S09 영구 기절 락 | **채택** | 기절 후 경직 내성 1턴 + S09 유니크 + 기믹은 기절 무시(§3.2, §3.7) |
| 4 | X04 다중 채용 | **채택(수정)** | 유니크 + 증정 카드 런 제외. **배율 ×3 하향안은 기각** — 실비용 부여로 충분, 이중 너프는 카드 사장 |
| 5 | 바이럴 크리 지수 증폭 | **채택(수정)** | 가산 상한 +12 + 버프 가산 제출당 1회(§3.3, §3.5). **"크리 런당 1회" 안은 기각** — 성향 정체성 훼손 |
| 6 | 감성 디버프 슬롯 담합 | **채택** | R20 캡 +3 확정 + 동일 계열 슬롯 효과 1개로 강화(§6.2) |
| 7 | 좋아요 품앗이 링 | **채택(단계화)** | 첫 보스 처치 게이트 v1, 클러스터 감쇠·그래프 탐지 v1.1(§6.2) |
| 8 | 자살 파밍 루프 | **채택(수정)** | 사망 RP = 신규 최고 도달 막만, 업보 골드 막 비례(도입 자체 연기), 유언 보상은 전투 승리 1회 조건. **"유언 작성권 보스방 도달" 게이트는 기각** — 유언은 비동기 매력의 최전선, 작성 자체는 막지 않는다(§4.3) |

### A-2. 신규 유저 온보딩 관점

| # | 지적 | 판정 | 반영/근거 |
|---|---|---|---|
| 1 | 버프 태그 판정 1판 노출 | **채택** | 1판 무판정, 2판 개방, 대상 아이콘(§3.3, §4.4) |
| 2 | 계열 콤보 컷 | **채택** | v1.0 폐지. 게이지 재검증 §3.4 — 01 제안④는 폐기 |
| 3 | 헛소리 원인 비가시성 | **채택(확대)** | "1막 한정" 제한은 기각하고 **전 구간 상시 노출**(§3.3) |
| 4 | 성향 UI 조기 노출 | **채택** | 2판 해금, 1판은 필살 버튼 축약(§3.5) |
| 5 | 항복 1판 배치 과잉 | **채택** | 2판 E02에서 자연 발견(§4.4) |
| 6 | 카드 텍스트 자립도 | **채택** | 고정 서식·무판정 뱃지·S03 재서술·X08 개명 「별점 구걸」(§3.7) |
| 7 | 게이지 감소 카드 초반 배제 | **채택(유지 확정)** | 시작 덱·1막 보상 풀 제외 명문화(§3.6) |

### A-3. 1인 개발 실현성 관점

| # | 지적 | 판정 | 반영/근거 |
|---|---|---|---|
| 1 | Layer1 ≠ MVP | **채택** | MVP = 1막 완결(§1.2). 단 시작 덱은 05의 10장안이 아니라 01의 12장안(R06과 정합) |
| 2 | 🔴 8건 미해소 | **채택** | 본 문서에서 R01~R23 전건 승인·반영(부록 B) |
| 3 | 메타 과잉 | **채택** | 재화 2계통(R03), 시즌·티어·탑100 v1.1 연기(§4.2) |
| 4 | 배당 구현비 | **채택** | Layer 3 v1 = 슬롯+좋아요+답글+유언, 배당·전단지·주간시드 v1.1(§6). 비용 상한 월 10만원(§1.1) |
| 5 | UGC 4구간 과욕 | **채택** | v1 = 금지어 팩폭만, 사기극 컷(§7) — X19 인플레 쟁점도 함께 소멸 |
| 6 | 기술 스택 | **채택** | TS+React/Svelte, 코어 분리, Tauri/Godot, Unity 배제(§1.1) |
| 7 | 컷/연기 목록 | **채택(1건 수정)** | 계정 골드·사기극·조롱·봉인/공양·오프라인 분리 컷, 업보·주간시드·티어·박제·엔딩 B/C·장인 연기. **네고·슬롯 경쟁·금지어 팩폭 유지.** E09는 수리안(첫 팩트 −1) 대신 컷 채택 — 수리해도 재미 기여 낮음 |

## 10. 부록 B — 충돌 해소(R01~R23) 승인 현황

전건 **승인**. 수정 승인 3건만 기재:
- **R05**: Tier계수에 승리 작성권용 0.8 단계 추가(§6.2).
- **R11**: B01 48 하향은 Layer 1.5 시점으로 이연 — MVP는 60 유지(1막 완결 런의 최종 보스 역할).
- **E09 대응**: 10의 수리안 대신 조롱 버튼 자체를 컷(실현성 검증 #7 우선).

주요 확정 요약: 시작 덱 = 01 12장안(R06) / 제거 25G+10 상한45(R07) / 갈취 8/15/25(R08) / 휴식 30%(R09) / 유언 L3 템플릿·L4 자유문(R10) / 시즌 12주+주간 라운드(R12) / "리뷰어 랭크"(R13) / 쿼터 min(랭크,5)(R14) / 부착 슬롯 공유(R15) / 진열대 45G 상한(R16) / 패러디 일반화 치환만(R17) / 답글 "정지"+3등급 인증(R18) / "필력" 채택·사전 참조 조건(R19) / 외부 보정 캡(R20) / X09 41장 증설(R21) / 답글 대상 출처 무관 최상위(R22) / 장비 스키마 합집합(R23).

## 11. TBD (사유·기한 명시)

| 항목 | 사유 | 결정 기한 |
|---|---|---|
| 2·3막 보스 2종 + 일반 적 4~6종 데이터 | MVP 범위 밖, 물량 작업 | Layer 1.5 착수 시 |
| ~~B01 48 하향 확정~~ | **해소(v1.1)** — 페이즈2 비례 트리거 동반으로 재시뮬 통과 | 완료 |
| 승률 인플레 재조정 (밸런스 라운드 2) | v1.1 적용 후 표준 B01 승률 83.2%로 초회 기준(30~50%) 대폭 초과, 억지 처벌 여전 미달 — 손패·덱 구성 레버 또는 적 화력 상향 필요 (balance-report-v2 §5) | 라운드 2 착수 시 |
| 3막 보스 = 「베스트 리뷰어」 (worldview §1.4에서 해소, X16) | 서사·크리 밸런스 동시 결정 사안 | 3막 시나리오 집필 시 |
| 전단지 열람 수치 보정(X18) | 전단지 자체가 v1.1 | Layer 3 v1.1 착수 시 |
| 주간 시드 점수식 E04 계수(전투 승수 ×30, X07 층 가점 절반) | 실플레이 데이터 필요 | 주간 시드 도입 시 |
| 엔딩 C 입력 리뷰 LLM 구간(X34) | 엔딩 C 자체가 보류 | Layer 4 v1.1 이후 |
